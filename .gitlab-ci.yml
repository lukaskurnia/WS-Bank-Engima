image: maven:3-jdk-8

before_script:
    - 'which ssh-agent || ( apt-get update -y && apt-get install openssh-client -y )'

stages:
    - lint_build
    - deploy

lint:
    stage: lint_build
    script:
        - mvn package
